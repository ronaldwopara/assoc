export type PopupCmsData = {
  version: 1;
  updatedAt: string;
  enabled: boolean;
  imageUrl: string;
  imageRatio: number;
  buttonLabel: string;
  buttonHref: string;
  /** Hex color for the CTA footer strip under the flyer (e.g. #ffffff). */
  footerColor: string;
};
