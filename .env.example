# Google reCAPTCHA v2 (“I’m not a robot” checkbox).
# Add asosc.ca, www.asosc.ca, and localhost for local testing.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Staff gate for /upgrade CMS + future /dashboard (server-only)
UPGRADE_PASSWORD=
# Optional; defaults to UPGRADE_PASSWORD if unset
# UPGRADE_SESSION_SECRET=

# Cloudinary (image uploads + CMS JSON storage)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Google OAuth (Interac email + Drive invoices + Sheets)
# Create at console.cloud.google.com → Google Auth Platform → Clients
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Must match an Authorized redirect URI on the OAuth client
GOOGLE_REDIRECT_URI=http://localhost:4000/api/google/callback
# AES key material for encrypting the refresh token in Cloudinary (openssl rand -base64 32)
GOOGLE_TOKEN_SECRET=
